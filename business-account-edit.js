// NR BizPro — editable business account profile
(function(){
 const CATEGORIES=['General Business','Grocery / General Store','Footwear / Chappal Shop','Fertilizer / Agriculture','Garage / Service Center','Spare Parts','EV Two-Wheeler Showroom','Retail / Supermarket','Restaurant / Bakery','Hardware / Building Materials','Medical / Pharmacy','Electronics / Mobile','Clothing / Fashion','Furniture','Jewellery','Stationery / Book Store','Dairy / Milk Products','Salon / Beauty Parlour','Printing / Xerox / Online Services','Wholesale / Distributor','Professional Services','Construction / Building Materials','Paint Shop','Plumbing Business','Paint Shop + Plumbing Business','Other Business'];
 const esc=v=>String(v??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;','\\':'&#39;'}[m]));
 function users(){try{return JSON.parse(localStorage.getItem('nr-bizpro-users-v1')||'[]')}catch(e){return[]}}
 function saveUsers(list){localStorage.setItem('nr-bizpro-users-v1',JSON.stringify(list))}
 function addButton(){const panel=document.getElementById('settings');if(!panel||document.getElementById('editBusinessAccount'))return;const old=panel.querySelector('button[onclick="saveSettings()"]');const b=document.createElement('button');b.id='editBusinessAccount';b.className='secondary';b.type='button';b.textContent='Edit Business Account';b.style.marginLeft='10px';b.onclick=openEdit;if(old)old.insertAdjacentElement('afterend',b);else panel.appendChild(b)}
 function openEdit(){if(typeof currentUser==='undefined'||!currentUser)return alert('Please login first.');const modal=document.getElementById('modal');if(!modal)return;const cat=currentUser.category||state?.settings?.category||'General Business';modalTitle.textContent='Edit Business Account';modalBody.innerHTML=`<div class="form-grid"><label>Business Name<input id="eaBusiness" value="${esc(currentUser.business)}" required></label><label>Owner Name<input id="eaOwner" value="${esc(currentUser.owner)}" required></label><label>Mobile<input id="eaMobile" value="${esc(currentUser.mobile)}" required inputmode="tel"></label><label>Email<input id="eaEmail" value="${esc(currentUser.email)}" required type="email"></label><label>Business Category<select id="eaCategory">${CATEGORIES.map(x=>`<option value="${esc(x)}" ${x===cat?'selected':''}>${esc(x)}</option>`).join('')}</select></label><label>GSTIN (optional)<input id="eaGst" value="${esc(currentUser.gst||'')}"></label><label class="wide">Business Address<textarea id="eaAddress" rows="3">${esc(state?.settings?.address||'')}</textarea></label></div><div class="modal-actions"><button class="secondary" type="button" onclick="closeModal()">Cancel</button><button class="primary" type="button" id="saveBusinessAccount">Save Changes</button></div>`;modal.classList.remove('hidden');document.getElementById('saveBusinessAccount').onclick=saveEdit}
 function saveEdit(){const business=document.getElementById('eaBusiness').value.trim(),owner=document.getElementById('eaOwner').value.trim(),mobile=document.getElementById('eaMobile').value.trim(),email=document.getElementById('eaEmail').value.trim().toLowerCase(),category=document.getElementById('eaCategory').value,gst=document.getElementById('eaGst').value.trim(),address=document.getElementById('eaAddress').value.trim();if(!business||!owner||!mobile||!email)return alert('Please fill all required fields.');const list=users();if(list.some(u=>u.id!==currentUser.id&&(u.email===email||u.mobile===mobile)))return alert('Email or mobile is already used by another business account.');currentUser.business=business;currentUser.owner=owner;currentUser.mobile=mobile;currentUser.email=email;currentUser.category=category;currentUser.gst=gst;const idx=list.findIndex(u=>u.id===currentUser.id);if(idx<0)return alert('Business account not found.');list[idx]=currentUser;saveUsers(list);state.settings.name=business;state.settings.category=category;state.settings.mobile=mobile;state.settings.gst=gst;state.settings.address=address;if(typeof save==='function')save();if(typeof loadSettings==='function')loadSettings();if(typeof updateStats==='function')updateStats();window.dispatchEvent(new Event('loginSuccess'));closeModal();alert('Business account details updated successfully.');}
 function boot(){addButton();setTimeout(addButton,500);setTimeout(addButton,1500)}
 window.NRBizProBusinessAccountEdit={openEdit,saveEdit};window.addEventListener('load',boot);window.addEventListener('authReady',addButton);window.addEventListener('loginSuccess',addButton);setTimeout(boot,900);
})();
