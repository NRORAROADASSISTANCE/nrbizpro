async function api(path,opts={}){const r=await fetch(path,{credentials:'same-origin',cache:'no-store',headers:{'Content-Type':'application/json',...(opts.headers||{})},...opts});const j=await r.json().catch(()=>({}));if(!r.ok){const e=new Error(j.error||'Request failed');e.status=r.status;throw e}return j}
function togglePassword(id,btn){const input=document.getElementById(id);if(!input)return;const visible=input.type==='text';input.type=visible?'password':'text';btn.textContent=visible?'◉':'●';btn.setAttribute('aria-label',visible?'Show password':'Hide password')}
function addFinalPlanField(){const form=document.getElementById('createForm');if(!form||document.getElementById('adminPlan'))return;const wrap=document.createElement('div');wrap.className='field full';wrap.innerHTML='<label>Membership Plan *</label><select id="adminPlan" required><option value="">Select Final Plan</option><option value="year3">6 Years — ₹6,000</option><option value="lifetime">Lifetime — ₹15,000</option></select><span class="hint">Final plans: ₹6,000 for 6 years • ₹15,000 for lifetime.</span>';const status=document.getElementById('status');form.insertBefore(wrap,status)}
async function init(){for(let i=0;i<3;i++){try{await api('/api/admin?action=session');addFinalPlanField();return}catch(err){if(err.status===401){location.replace('/admin.html');return}await new Promise(r=>setTimeout(r,500))}}document.getElementById('status').textContent='Unable to verify Admin session. Please refresh and try again.'}
async function createBusiness(e){e.preventDefault();const status=document.getElementById('status'),btn=document.getElementById('createBtn');const password=document.getElementById('password').value,confirmPassword=document.getElementById('confirmPassword').value,plan=document.getElementById('adminPlan')?.value;if(password!==confirmPassword){status.textContent='Passwords do not match.';return}if(!plan){status.textContent='Please select a membership plan.';return}btn.disabled=true;btn.textContent='Creating...';status.textContent='';try{const r=await api('/api/admin',{method:'POST',body:JSON.stringify({action:'create-business',userId:document.getElementById('loginId').value.trim().toLowerCase(),business:document.getElementById('business').value.trim(),owner:document.getElementById('owner').value.trim(),mobile:document.getElementById('mobile').value.trim(),email:document.getElementById('email').value.trim().toLowerCase(),category:document.getElementById('category').value.trim(),address:document.getElementById('address').value.trim(),gst:document.getElementById('gst').value.trim().toUpperCase(),password})});await api('/api/admin-plan',{method:'POST',body:JSON.stringify({action:'activate-created',businessId:r.businessId,plan})});document.getElementById('createdLogin').textContent=r.loginId||r.userId;document.getElementById('createdPassword').textContent=password;const planText=plan==='lifetime'?'Lifetime — ₹15,000':'6 Years — ₹6,000';const p=document.querySelector('#successBox p');if(p)p.textContent=`Business account is ACTIVE on the selected final plan: ${planText}. Give these login details to the business holder.`;status.textContent=`Business created and activated: ${planText}.`;document.getElementById('createForm').style.display='none';document.getElementById('successBox').style.display='block'}catch(err){status.textContent=err.message}finally{btn.disabled=false;btn.textContent='Create Business'}}
init();