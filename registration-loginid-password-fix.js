(function(){'use strict';
function eye(id){return `<button type="button" class="nr-eye" data-target="${id}" aria-label="Show password">👁</button>`}
function pwdField(id,label,autocomplete){return `<label class="field nr-password-field">${label}<span class="nr-password-wrap"><input id="${id}" required type="password" autocomplete="${autocomplete}" minlength="8"><span class="nr-eye-slot">${eye(id)}</span></span></label>`}
function renderSignup(){const el=document.getElementById('authContent');if(!el)return;el.innerHTML=`<div class="auth-title"><h1>Create your business account</h1><p>Register once and get your unique Login ID.</p></div><form id="nrSignupForm"><div class="auth-grid"><label>Business Name<input id="suBusiness" required></label><label>Owner Name<input id="suOwner" required></label><label>Login ID<input id="suLoginId" required minlength="4" maxlength="40" pattern="[A-Za-z0-9._-]{4,40}" autocomplete="username" placeholder="e.g. ravindar.biz"></label><label>Mobile<input id="suMobile" required inputmode="tel" autocomplete="tel"></label><label>Email<input id="suEmail" required type="email" autocomplete="email"></label><label>Business Category<input id="suCategory" required placeholder="Garage / Retail / Service..."></label><label class="wide">Business Address<input id="suAddress" required autocomplete="street-address"></label><label>GSTIN <span>(optional)</span><input id="suGst"></label></div>${pwdField('suPassword','Password','new-password')}${pwdField('suConfirmPassword','Confirm Password','new-password')}<div id="nrSignupMsg" class="notice" style="display:none"></div><button class="primary auth-btn" type="submit">Continue to Plans</button></form><p class="auth-switch">Already registered? <button type="button" onclick="renderAuth('login')">Login</button></p>`;bindEyes();document.getElementById('nrSignupForm').onsubmit=signupFixed}
function bindEyes(){document.querySelectorAll('.nr-eye').forEach(b=>b.onclick=()=>{const i=document.getElementById(b.dataset.target);if(!i)return;i.type=i.type==='password'?'text':'password';b.textContent=i.type==='password'?'👁':'🙈';b.setAttribute('aria-label',i.type==='password'?'Show password':'Hide password')})}
async function signupFixed(e){e.preventDefault();const msg=document.getElementById('nrSignupMsg');const v=id=>document.getElementById(id)?.value.trim()||'';const password=document.getElementById('suPassword').value,confirm=document.getElementById('suConfirmPassword').value;msg.style.display='none';if(password.length<8){msg.textContent='Password must be at least 8 characters.';msg.style.display='block';return}if(password!==confirm){msg.textContent='Password and Confirm Password do not match.';msg.style.display='block';return}const payload={action:'signup',business:v('suBusiness'),owner:v('suOwner'),userId:v('suLoginId'),mobile:v('suMobile'),email:v('suEmail').toLowerCase(),category:v('suCategory'),address:v('suAddress'),gst:v('suGst'),password};try{const r=await fetch('/api/auth?action=signup',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify(payload)});const d=await r.json();if(!r.ok){msg.textContent=d.error||'Registration failed.';msg.style.display='block';return}window.currentUser=d.user;window.renderAuth?.('plans');}catch(err){msg.textContent='Server connection failed. Please try again.';msg.style.display='block'}}
const old=window.renderAuth;window.renderAuth=function(mode='login',message=''){if(mode==='signup')return renderSignup();if(typeof old==='function')old(mode,message);if(mode==='login')setTimeout(bindEyes,0)};
window.addEventListener('load',()=>{const s=document.createElement('style');s.textContent='.nr-password-wrap{position:relative;display:block}.nr-password-wrap input{padding-right:46px;width:100%;box-sizing:border-box}.nr-eye-slot{position:absolute;right:8px;top:50%;transform:translateY(-50%)}.nr-eye{border:0;background:transparent;cursor:pointer;font-size:18px;padding:5px;line-height:1}.nr-password-field{display:block}';document.head.appendChild(s);});
})();