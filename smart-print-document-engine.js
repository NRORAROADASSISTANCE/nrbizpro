/* NR BizPro Smart Print — real in-page document scanner/xerox engine. */
(function(){
  const MAX_SIDE=2600,$=id=>document.getElementById(id),clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
  const load=src=>new Promise((resolve,reject)=>{const im=new Image();im.onload=()=>resolve(im);im.onerror=reject;im.src=src});
  function canvasClean(c,mode){const ctx=c.getContext('2d'),w=c.width,h=c.height,img=ctx.getImageData(0,0,w,h),d=img.data;let sum=0;const gray=new Uint8Array(w*h);for(let i=0,p=0;i<d.length;i+=4,p++){const g=d[i]*.299+d[i+1]*.587+d[i+2]*.114;gray[p]=g;sum+=g}const avg=sum/gray.length;for(let p=0,i=0;p<gray.length;p++,i+=4){let g=clamp((gray[p]-(avg-205))*1.22+205,0,255);if(mode==='Black & White')g=g>210?255:g<75?0:g;d[i]=d[i+1]=d[i+2]=g;d[i+3]=255}ctx.putImageData(img,0,0);return c}
  function orderQuad(pts){const s=pts.map(p=>p.x+p.y),d=pts.map(p=>p.x-p.y);return[pts[s.indexOf(Math.min(...s))],pts[d.indexOf(Math.max(...d))],pts[s.indexOf(Math.max(...s))],pts[d.indexOf(Math.min(...d))]]}
  function detectQuad(src){const scale=Math.min(1,1400/Math.max(src.cols,src.rows)),small=new cv.Mat();cv.resize(src,small,new cv.Size(0,0),scale,scale,cv.INTER_AREA);const gray=new cv.Mat(),blur=new cv.Mat(),edge=new cv.Mat();cv.cvtColor(small,gray,cv.COLOR_RGBA2GRAY);cv.GaussianBlur(gray,blur,new cv.Size(5,5),0);cv.Canny(blur,35,120,edge);const contours=new cv.MatVector(),hier=new cv.Mat();cv.findContours(edge,contours,hier,cv.RETR_LIST,cv.CHAIN_APPROX_SIMPLE);let best=null,bestScore=0,frame=small.rows*small.cols;for(let i=0;i<contours.size();i++){const cnt=contours.get(i),peri=cv.arcLength(cnt,true),approx=new cv.Mat();cv.approxPolyDP(cnt,approx,.02*peri,true);const area=Math.abs(cv.contourArea(approx));if(approx.rows===4&&area>frame*.30){const pts=[];for(let j=0;j<4;j++){const q=approx.intPtr(j,0);pts.push({x:q[0]/scale,y:q[1]/scale})}const p=orderQuad(pts),width=Math.max(Math.hypot(p[1].x-p[0].x,p[1].y-p[0].y),Math.hypot(p[2].x-p[3].x,p[2].y-p[3].y)),height=Math.max(Math.hypot(p[3].x-p[0].x,p[3].y-p[0].y),Math.hypot(p[2].x-p[1].x,p[2].y-p[1].y)),ratio=width/Math.max(1,height);if(ratio<4&&ratio>.15){const margin=Math.min(...pts.map(q=>Math.min(q.x,q.y,src.cols-q.x,src.rows-q.y)));const touches=(margin<Math.min(src.cols,src.rows)*.01);const score=area*(touches?.75:1);if(score>bestScore){best=pts;bestScore=score}}}approx.delete();cnt.delete()}small.delete();gray.delete();blur.delete();edge.delete();contours.delete();hier.delete();return bestScore>frame*.30?best:null}
  function warp(src,quad){if(!quad)return src;const p=orderQuad(quad),w=Math.round(Math.max(Math.hypot(p[1].x-p[0].x,p[1].y-p[0].y),Math.hypot(p[2].x-p[3].x,p[2].y-p[3].y))),h=Math.round(Math.max(Math.hypot(p[3].x-p[0].x,p[3].y-p[0].y),Math.hypot(p[2].x-p[1].x,p[2].y-p[1].y)));if(w<150||h<150)return src;const sp=cv.matFromArray(4,1,cv.CV_32FC2,[p[0].x,p[0].y,p[1].x,p[1].y,p[2].x,p[2].y,p[3].x,p[3].y]),dp=cv.matFromArray(4,1,cv.CV_32FC2,[0,0,w-1,0,w-1,h-1,0,h-1]),M=cv.getPerspectiveTransform(sp,dp),dst=new cv.Mat();cv.warpPerspective(src,dst,M,new cv.Size(w,h),cv.INTER_CUBIC,cv.BORDER_CONSTANT,new cv.Scalar(255,255,255,255));sp.delete();dp.delete();M.delete();return dst}
  function flatten(src){const gray=new cv.Mat(),bg=new cv.Mat(),norm=new cv.Mat();cv.cvtColor(src,gray,cv.COLOR_RGBA2GRAY);const k=cv.getStructuringElement(cv.MORPH_ELLIPSE,new cv.Size(41,41));cv.morphologyEx(gray,bg,cv.MORPH_CLOSE,k);cv.divide(gray,bg,norm,255,cv.CV_8U);const clahe=new cv.CLAHE(2,new cv.Size(8,8));clahe.apply(norm,norm);const out=new cv.Mat();cv.cvtColor(norm,out,cv.COLOR_GRAY2RGBA);gray.delete();bg.delete();k.delete();clahe.delete();return out}
  async function process(srcData,mode){const im=await load(srcData),base=document.createElement('canvas'),scale=Math.min(1,MAX_SIDE/Math.max(im.naturalWidth||im.width,im.naturalHeight||im.height));base.width=Math.round((im.naturalWidth||im.width)*scale);base.height=Math.round((im.naturalHeight||im.height)*scale);base.getContext('2d').drawImage(im,0,0,base.width,base.height);if(typeof cv==='undefined'||!cv.Mat)return{data:canvasClean(base,mode).toDataURL('image/jpeg',.94),corrected:false,cropped:false,fallback:true};let src=null,warped=null,final=null,out=null;try{src=cv.imread(base);const quad=detectQuad(src);warped=quad?warp(src,quad):src;final=flatten(warped);out=final;if(mode==='Black & White'){const g=new cv.Mat(),bw=new cv.Mat();cv.cvtColor(final,g,cv.COLOR_RGBA2GRAY);cv.adaptiveThreshold(g,bw,255,cv.ADAPTIVE_THRESH_GAUSSIAN_C,cv.THRESH_BINARY,31,9);out=new cv.Mat();cv.cvtColor(bw,out,cv.COLOR_GRAY2RGBA);g.delete();bw.delete()}const c=document.createElement('canvas');c.width=out.cols;c.height=out.rows;cv.imshow(c,out);const data=c.toDataURL('image/png');const cropped=!!quad;src.delete();if(warped&&warped!==src)try{warped.delete()}catch{}if(final&&final!==out)final.delete();if(out&&out!==final)out.delete();return{data,corrected:cropped,cropped,fallback:false}}catch(e){try{if(src)src.delete()}catch{}return{data:canvasClean(base,mode).toDataURL('image/jpeg',.94),corrected:false,cropped:false,fallback:true}}}
  function install(){if(window.__smartPrintDocumentEngineInstalled)return;window.__smartPrintDocumentEngineBooting=true;const old=window.prepareProcessedPages;if(typeof old!=='function'){setTimeout(install,250);return}async function wrapped(){if(typeof window.type!=='undefined'&&window.type!=='document'){await old();return}if(!window.sourcePages?.length){await old();return}window.processedPages=[];let cropped=0,fallback=0;for(const src of window.sourcePages){const r=await process(src,$('mode')?.value||'Black & White');window.processedPages.push(r.data);if(r.cropped)cropped++;if(r.fallback)fallback++}const hint=$('typeHint');if(hint)hint.textContent=`✓ ${window.processedPages.length} print-ready page${window.processedPages.length>1?'s':''} • ${cropped?'auto crop + perspective correction • ':''}shadow removal • xerox cleanup • original untouched${fallback?' • compatibility cleanup used':''}.`}wrapped.__documentEngine=true;window.prepareProcessedPages=wrapped;window.__smartPrintDocumentEngineInstalled=true;window.__smartPrintDocumentEngineBooting=false}
  function boot(){if(typeof cv!=='undefined'&&cv.Mat)install();else setTimeout(boot,300)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();