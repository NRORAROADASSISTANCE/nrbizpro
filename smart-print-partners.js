/* NR BizPro Smart Print — partner UI aligned with final commercial model. */
(function(){
 function mount(){if(document.getElementById('spPartnerPanel'))return;const p=document.createElement('section');p.id='spPartnerPanel';p.className='card';const host=document.querySelector('#workspace')||document.body;p.innerHTML='<h2>🤝 Smart Print Partner & License</h2><div class="form"><label>Partner ID<input id="spPartnerId" placeholder="Partner ID"></label><label>Role<select id="spPartnerRole"><option value="distributor">Distributor</option><option value="retailer">Retailer</option></select></label></div><div class="actions"><button class="primary" id="spRegister">Register — ₹2,000 / 3 Years</button><button class="secondary" id="spRenew">Renew — ₹1,000 / 1 Year</button></div><p class="muted">Applicable transaction platform fee: ₹200. Legacy points/recharge model is retired.</p><div id="spPartnerStatus"></div>';host.appendChild(p);render();document.getElementById('spRegister').onclick=function(){try{nrSmartPrintBusiness.createAccount(document.getElementById('spPartnerId').value.trim(),document.getElementById('spPartnerRole').value);render()}catch(e){alert(e.message)}};document.getElementById('spRenew').onclick=function(){try{nrSmartPrintBusiness.renew(document.getElementById('spPartnerId').value.trim());render()}catch(e){alert(e.message)}}}
 function render(){const id=document.getElementById('spPartnerId')?.value.trim();const d=nrSmartPrintBusiness.dashboard(),a=id&&d.accounts[id];const s=document.getElementById('spPartnerStatus');if(!s)return;s.innerHTML=a?'<b>Status:</b> '+a.status+' &nbsp; <b>Expires:</b> '+new Date(a.expiresAt).toLocaleDateString():'<span class="muted">Enter a Partner ID to view license status.</span>'}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(mount,700));else setTimeout(mount,700);
})();